[package]
name = "node-std"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Full Node da blockchain com suporte à std para ESP32-S3"

[dependencies]
blockchain-core = { path = "../core", features = ["std"] }
